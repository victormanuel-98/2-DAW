<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiAppDAW.Views.MainPage"
             Title="Star Wars API">

    <VerticalStackLayout Padding="20" Spacing="15">
        <Label Text="Buscar personaje" Style="{StaticResource SW-Title}" />

        <Frame Style="{StaticResource SW-Card}">
            <VerticalStackLayout Spacing="10">
                <Entry x:Name="txtName" Placeholder="Escribe el nombre..." />
                <HorizontalStackLayout Spacing="10">
                    <Button Text="Buscar" Clicked="OnSearchClicked" Style="{StaticResource SW-AccentButton}" />
                    <Button Text="Guardados" Clicked="OnSavedClicked" Style="{StaticResource SW-AccentButton}" />
                </HorizontalStackLayout>
            </VerticalStackLayout>
        </Frame>

        <Frame Style="{StaticResource SW-Card}">
            <Label x:Name="lblResult" TextColor="{StaticResource White}" />
        </Frame>
    </VerticalStackLayout>
</ContentPage>
